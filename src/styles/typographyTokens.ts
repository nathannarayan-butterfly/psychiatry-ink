/** Keep in sync with --font-sans / --font-mono / display tokens in globals.css @theme + :root. */
export const FONT_SANS = '"Inter", system-ui, -apple-system, sans-serif'
export const FONT_MONO = 'ui-monospace, "SF Mono", "Cascadia Code", monospace'
/** Display role uses the sans stack; hierarchy is weight/size via CSS tokens. */
export const FONT_DISPLAY = FONT_SANS
