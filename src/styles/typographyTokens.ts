/** Keep in sync with --font-sans / --font-mono in globals.css @theme. */
export const FONT_SANS = '"Inter", system-ui, -apple-system, sans-serif'
export const FONT_MONO = 'ui-monospace, "SF Mono", "Cascadia Code", monospace'
