/**
 * Parser version — bump when a parser adapter changes how it maps source content
 * to candidates. Stamped into every envelope and every provenance record so that
 * imported items remain traceable to the exact parsing logic that produced them.
 */
export const DOCUMENT_IMPORT_PARSER_VERSION = '1.0.0'
