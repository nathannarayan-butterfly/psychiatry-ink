{
  "_about": "Cursor-native port of Anthropic's claude-code security-guidance Layer 1 (deterministic per-edit pattern warnings). The first 25 rules (origin='anthropic') are faithful ports of the plugin's SECURITY_PATTERNS; the remaining rules (origin='psychiatry-ink') are additive deterministic checks requested for this repo. To extend WITHOUT editing this file, drop a security-patterns.local.json next to it with the same {\"patterns\": [...]} shape (additive only).",
  "_schema": {
    "ruleName": "unique id (string, required)",
    "severity": "critical|high|medium (string)",
    "regex": "optional Python re pattern (matched with re.search over new content)",
    "substrings": "optional list of literal substrings (any match fires)",
    "include_exts": "optional list of file extensions; rule only runs on these",
    "exclude_exts": "optional list of file extensions to skip",
    "path_contains": "optional substring that must appear in the file path",
    "reminder": "the warning text surfaced to the agent (string, required)"
  },
  "patterns": [
    {
      "ruleName": "github_actions_workflow",
      "origin": "anthropic",
      "severity": "high",
      "path_contains": ".github/workflows/",
      "include_exts": [
        ".yml",
        ".yaml"
      ],
      "reminder": "⚠️ Security Warning: You are editing a GitHub Actions workflow file. Be aware of these security risks:\n\n1. **Command Injection**: Never use untrusted input (like issue titles, PR descriptions, commit messages) directly in run: commands without proper escaping\n2. **Use environment variables**: Instead of ${{ github.event.issue.title }}, use env: with proper quoting\n3. **Review the guide**: https://github.blog/security/vulnerability-research/how-to-catch-github-actions-workflow-injections-before-attackers-do/\n\nExample of UNSAFE pattern to avoid:\nrun: echo \"${{ github.event.issue.title }}\"\n\nExample of SAFE pattern:\nenv:\n  TITLE: ${{ github.event.issue.title }}\nrun: echo \"$TITLE\"\n\nOther risky inputs to be careful with:\n- github.event.issue.body\n- github.event.pull_request.title / body\n- github.event.comment.body\n- github.event.review.body / review_comment.body\n- github.event.commits.*.message / author.email / author.name\n- github.event.head_commit.message / author.email / author.name\n- github.event.pull_request.head.ref / label / repo.default_branch\n- github.event.client_payload.* (repository_dispatch — attacker can set any field)\n- github.head_ref\n\n4. **Ref injection**: Never use untrusted input in `ref:` parameters of `actions/checkout`. For `client_payload.pr_number`, validate it matches `^[0-9]+$` before using in `ref: refs/pull/${{ ... }}/head`"
    },
    {
      "ruleName": "child_process_exec",
      "origin": "anthropic",
      "severity": "high",
      "include_exts": [
        ".js",
        ".jsx",
        ".ts",
        ".tsx",
        ".mjs",
        ".cjs",
        ".mts",
        ".cts",
        ".vue",
        ".svelte"
      ],
      "substrings": [
        "child_process.exec",
        "execSync("
      ],
      "regex": "(?<![a-zA-Z0-9_\\.])exec\\(",
      "reminder": "⚠️ Security Warning: Using child_process.exec() can lead to command injection vulnerabilities.\n\nexec() runs the command string through a shell, so any user input interpolated into it can inject arbitrary commands. Prefer child_process.execFile() (or spawn()) with an argument array instead of building a shell string.\n\nInstead of:\n  exec(`command ${userInput}`)\n\nUse:\n  import { execFile } from 'node:child_process'\n  execFile('command', [userInput], callback)\n\nOnly use exec() if you absolutely need shell features and the input is guaranteed to be safe."
    },
    {
      "ruleName": "new_function_injection",
      "origin": "anthropic",
      "severity": "high",
      "substrings": [
        "new Function"
      ],
      "reminder": "⚠️ Security Warning: Using new Function() with string interpolation is a CODE INJECTION vulnerability. If any variable is concatenated or interpolated into the function body string, an attacker controlling that variable can execute arbitrary code. Use safe alternatives: for property access use obj[key] or array.reduce((o, k) => o[k], root); for computation use a safe expression parser. NEVER interpolate untrusted strings into new Function() bodies."
    },
    {
      "ruleName": "eval_injection",
      "origin": "anthropic",
      "severity": "high",
      "exclude_exts": [
        ".md",
        ".mdx",
        ".txt",
        ".rst",
        ".json",
        ".yaml",
        ".yml"
      ],
      "regex": "(?<![a-zA-Z0-9_\\.])eval\\(",
      "reminder": "⚠️ Security Warning: eval() executes arbitrary code and is a major security risk. Use JSON.parse() for data, ast.literal_eval() for Python literals, or a safe expression parser. If this is safe or is explicitly needed, briefly document that in a comment before continuing."
    },
    {
      "ruleName": "react_dangerously_set_html",
      "origin": "anthropic",
      "severity": "high",
      "substrings": [
        "dangerouslySetInnerHTML"
      ],
      "reminder": "⚠️ Security Warning: dangerouslySetInnerHTML can lead to XSS vulnerabilities if used with untrusted content. Ensure all content is properly sanitized using an HTML sanitizer library like DOMPurify, or use safe alternatives."
    },
    {
      "ruleName": "document_write_xss",
      "origin": "anthropic",
      "severity": "medium",
      "substrings": [
        "document.write"
      ],
      "reminder": "⚠️ Security Warning: document.write() can be exploited for XSS attacks and has performance issues. Use DOM manipulation methods like createElement() and appendChild() instead."
    },
    {
      "ruleName": "innerHTML_xss",
      "origin": "anthropic",
      "severity": "high",
      "substrings": [
        ".innerHTML =",
        ".innerHTML="
      ],
      "reminder": "⚠️ Security Warning: Setting innerHTML with untrusted content can lead to XSS vulnerabilities. Use textContent for plain text or safe DOM methods for HTML content. If you need HTML support, consider using an HTML sanitizer library such as DOMPurify."
    },
    {
      "ruleName": "pickle_deserialization",
      "origin": "anthropic",
      "severity": "high",
      "include_exts": [
        ".py",
        ".pyi",
        ".ipynb"
      ],
      "regex": "(?<![a-zA-Z0-9_])pickle\\.(loads?|Unpickler)\\b|(?<![a-zA-Z0-9_])pkl_load\\(",
      "reminder": "⚠️ Security Warning: Loading pickle data (or equivalents: cPickle, cloudpickle, dill, marshal, shelve, joblib, pandas.read_pickle, numpy with allow_pickle=True) from untrusted sources allows arbitrary code execution.\n\nFor simple data, prefer JSON or msgspec. For typed objects, prefer a schema-validated deserializer (msgspec.Struct, pydantic, marshmallow) that constructs only declared types.\n\nIf this is safe or is explicitly needed, briefly document that in a comment before continuing."
    },
    {
      "ruleName": "os_system_injection",
      "origin": "anthropic",
      "severity": "high",
      "include_exts": [
        ".py",
        ".pyi",
        ".ipynb"
      ],
      "regex": "\\bos\\.system\\s*\\(",
      "substrings": [
        "from os import system"
      ],
      "reminder": "⚠️ Security Warning: os.system() runs a shell and is a command-injection sink. Use subprocess.run([...]) with a list of arguments instead. If this is safe or is explicitly needed, briefly document that in a comment before continuing."
    },
    {
      "ruleName": "python_subprocess_shell",
      "origin": "anthropic",
      "severity": "high",
      "regex": "subprocess\\.(?:run|call|Popen|check_output|check_call)\\(.*shell\\s*=\\s*True",
      "reminder": "⚠️ Security Warning: Using subprocess with shell=True enables command injection.\n\nUNSAFE:\n  subprocess.run(f\"ls {user_input}\", shell=True)\n  subprocess.call(\"grep \" + pattern, shell=True)\n\nSAFE - pass arguments as a list without shell:\n  subprocess.run([\"ls\", user_input])\n  subprocess.call([\"grep\", pattern])\n\nWhen arguments are passed as a list without shell=True, special characters cannot be interpreted as shell metacharacters."
    },
    {
      "ruleName": "go_exec_shell_injection",
      "origin": "anthropic",
      "severity": "high",
      "regex": "exec\\.Command\\(\\s*\"(?:sh|bash|/bin/sh|/bin/bash)\"",
      "reminder": "⚠️ Security Warning: Using exec.Command with a shell interpreter (sh/bash) enables command injection.\n\nUNSAFE:\n  exec.Command(\"sh\", \"-c\", \"ping -c 1 \" + host)\n\nSAFE - pass arguments directly without a shell:\n  exec.Command(\"ping\", \"-c\", \"1\", host)\n\nWhen arguments are passed directly (not through a shell), special characters in user input cannot be interpreted as shell metacharacters. Additionally validate inputs (net.ParseIP for hosts, filepath.Clean for paths, parse numerics first)."
    },
    {
      "ruleName": "unsafe_yaml_load",
      "origin": "anthropic",
      "severity": "high",
      "regex": "\\byaml\\.load\\s*\\((?![^)\\n]{0,80}\\bSafe)",
      "reminder": "⚠️ Security Warning: yaml.load() / yaml.unsafe_load() execute arbitrary Python via !!python/object tags.\n\nUse yaml.safe_load() if the file only contains simple data structures (dicts, lists, strings, numbers). If you need typed objects, parse with safe_load and validate the result against a schema (pydantic, msgspec, marshmallow) — never use a custom Loader that constructs arbitrary types."
    },
    {
      "ruleName": "node_createcipher_no_iv",
      "origin": "anthropic",
      "severity": "high",
      "regex": "\\bcrypto\\.(createCipher|createDecipher)\\b",
      "reminder": "⚠️ Security Warning: Use crypto.createCipheriv() / createDecipheriv(). createCipher was removed in Node 22 and derives the key insecurely (no IV, MD5-based KDF)."
    },
    {
      "ruleName": "aes_ecb_mode",
      "origin": "anthropic",
      "severity": "high",
      "regex": "\\bAES\\.MODE_ECB\\b|\\bmodes\\.ECB\\s*\\(|[\\x22\\x27]aes-\\d+-ecb[\\x22\\x27]",
      "reminder": "⚠️ Security Warning: Use AES-GCM or AES-CBC with HMAC. ECB mode leaks plaintext structure (identical blocks encrypt to identical ciphertext)."
    },
    {
      "ruleName": "tls_verification_disabled",
      "origin": "anthropic",
      "severity": "high",
      "regex": "\\bverify\\s*=\\s*False\\b|rejectUnauthorized\\s*:\\s*false|InsecureSkipVerify\\s*:\\s*true|NODE_TLS_REJECT_UNAUTHORIZED\\s*=\\s*[\\x22\\x27]?0|ssl\\._create_unverified_context|check_hostname\\s*=\\s*False",
      "reminder": "⚠️ Security Warning: Don't disable TLS verification. This allows MITM attacks. For self-signed dev certs, add the CA to your trust store or use a properly-issued cert."
    },
    {
      "ruleName": "marshal_loads",
      "origin": "anthropic",
      "severity": "high",
      "regex": "\\bmarshal\\.loads?\\s*\\(",
      "reminder": "⚠️ Security Warning: Loading pickle data (or equivalents: cPickle, cloudpickle, dill, marshal, shelve, joblib, pandas.read_pickle, numpy with allow_pickle=True) from untrusted sources allows arbitrary code execution.\n\nFor simple data, prefer JSON or msgspec. For typed objects, prefer a schema-validated deserializer (msgspec.Struct, pydantic, marshmallow) that constructs only declared types.\n\nIf this is safe or is explicitly needed, briefly document that in a comment before continuing."
    },
    {
      "ruleName": "shelve_open",
      "origin": "anthropic",
      "severity": "medium",
      "regex": "\\bshelve\\.open\\s*\\(",
      "reminder": "⚠️ Security Warning: Loading pickle data (or equivalents: cPickle, cloudpickle, dill, marshal, shelve, joblib, pandas.read_pickle, numpy with allow_pickle=True) from untrusted sources allows arbitrary code execution.\n\nFor simple data, prefer JSON or msgspec. For typed objects, prefer a schema-validated deserializer (msgspec.Struct, pydantic, marshmallow) that constructs only declared types.\n\nIf this is safe or is explicitly needed, briefly document that in a comment before continuing."
    },
    {
      "ruleName": "xml_unsafe_parse",
      "origin": "anthropic",
      "severity": "high",
      "regex": "\\b(xml\\.etree\\.ElementTree|ElementTree|ET)\\.(parse|fromstring|XML)\\s*\\(|\\bminidom\\.(parse|parseString)\\s*\\(|\\bxml\\.sax\\.(parse|make_parser)\\b",
      "reminder": "⚠️ Security Warning: Use defusedxml.ElementTree. Python's stdlib XML parsers are vulnerable to XXE (external entity) and billion-laughs attacks by default."
    },
    {
      "ruleName": "pickle_variants_load",
      "origin": "anthropic",
      "severity": "high",
      "regex": "\\b(cPickle|cloudpickle|dill)\\.(load|loads)\\s*\\(",
      "reminder": "⚠️ Security Warning: Loading pickle data (or equivalents: cPickle, cloudpickle, dill, marshal, shelve, joblib, pandas.read_pickle, numpy with allow_pickle=True) from untrusted sources allows arbitrary code execution.\n\nFor simple data, prefer JSON or msgspec. For typed objects, prefer a schema-validated deserializer (msgspec.Struct, pydantic, marshmallow) that constructs only declared types.\n\nIf this is safe or is explicitly needed, briefly document that in a comment before continuing."
    },
    {
      "ruleName": "outerHTML_xss",
      "origin": "anthropic",
      "severity": "high",
      "substrings": [
        ".outerHTML =",
        ".outerHTML="
      ],
      "reminder": "⚠️ Security Warning: Use textContent or sanitize with DOMPurify. outerHTML assignment is an XSS sink equivalent to innerHTML."
    },
    {
      "ruleName": "insertAdjacentHTML_xss",
      "origin": "anthropic",
      "severity": "high",
      "substrings": [
        ".insertAdjacentHTML("
      ],
      "reminder": "⚠️ Security Warning: Use insertAdjacentText() or sanitize with DOMPurify. insertAdjacentHTML is an XSS sink."
    },
    {
      "ruleName": "script_src_without_sri",
      "origin": "anthropic",
      "severity": "medium",
      "regex": "<script\\s+(?![^>]{0,400}integrity\\s*=)[^>]{0,200}src\\s*=\\s*[\\x22\\x27](?:https?:)?//[^\\x22\\x27]{1,300}[\\x22\\x27][^>]{0,100}>",
      "reminder": "⚠️ Security Warning: Add integrity=\"sha384-...\" crossorigin=\"anonymous\" to external script tags. Loading scripts without Subresource Integrity exposes you to CDN compromise."
    },
    {
      "ruleName": "torch_unsafe_load",
      "origin": "anthropic",
      "severity": "high",
      "regex": "(?:\\btorch\\.load|\\.torch_load)\\s*\\((?![^)\\n]{0,200}weights_only\\s*=\\s*True)",
      "reminder": "⚠️ Security Warning: torch.load() defaults to weights_only=False, which unpickles arbitrary Python objects and allows arbitrary code execution.\n\nIf the file only contains tensors and simple data structures, pass weights_only=True (or set TORCH_FORCE_WEIGHTS_ONLY_LOAD=1)."
    },
    {
      "ruleName": "yaml_unsafe_load_variants",
      "origin": "anthropic",
      "severity": "high",
      "regex": "(?:\\byaml\\.unsafe_load|\\.yaml_unsafe_load)\\s*\\(",
      "reminder": "⚠️ Security Warning: yaml.load() / yaml.unsafe_load() execute arbitrary Python via !!python/object tags.\n\nUse yaml.safe_load() if the file only contains simple data structures (dicts, lists, strings, numbers). If you need typed objects, parse with safe_load and validate the result against a schema (pydantic, msgspec, marshmallow) — never use a custom Loader that constructs arbitrary types."
    },
    {
      "ruleName": "pickle_wrapper_load",
      "origin": "anthropic",
      "severity": "high",
      "regex": "\\bjoblib\\.load\\s*\\(|\\b(?:pd|pandas)\\.read_pickle\\s*\\(|\\.cloudpickle_load\\s*\\(|\\b(?:np|numpy)\\.load\\s*\\([^)\\n]{0,200}allow_pickle\\s*=\\s*True",
      "reminder": "⚠️ Security Warning: Loading pickle data (or equivalents: cPickle, cloudpickle, dill, marshal, shelve, joblib, pandas.read_pickle, numpy with allow_pickle=True) from untrusted sources allows arbitrary code execution.\n\nFor simple data, prefer JSON or msgspec. For typed objects, prefer a schema-validated deserializer (msgspec.Struct, pydantic, marshmallow) that constructs only declared types.\n\nIf this is safe or is explicitly needed, briefly document that in a comment before continuing."
    },
    {
      "ruleName": "hardcoded_stripe_secret_key",
      "origin": "psychiatry-ink",
      "severity": "critical",
      "regex": "\\bsk_live_[0-9a-zA-Z]{16,}",
      "reminder": "⚠️ Security Warning: A live Stripe secret key (sk_live_...) appears to be hardcoded. Never commit live secrets. Move it to an environment variable / secret manager and rotate the exposed key immediately."
    },
    {
      "ruleName": "hardcoded_aws_access_key",
      "origin": "psychiatry-ink",
      "severity": "critical",
      "regex": "\\b(AKIA|ASIA|AGPA|AIDA|AROA|ANPA|ANVA)[0-9A-Z]{16}\\b",
      "reminder": "⚠️ Security Warning: A hardcoded AWS access key id was detected. Remove it, load credentials from the environment / instance role, and rotate the key if it was real."
    },
    {
      "ruleName": "private_key_material",
      "origin": "psychiatry-ink",
      "severity": "critical",
      "substrings": [
        "-----BEGIN RSA PRIVATE KEY-----",
        "-----BEGIN PRIVATE KEY-----",
        "-----BEGIN OPENSSH PRIVATE KEY-----",
        "-----BEGIN EC PRIVATE KEY-----",
        "-----BEGIN DSA PRIVATE KEY-----",
        "-----BEGIN PGP PRIVATE KEY BLOCK-----"
      ],
      "reminder": "⚠️ Security Warning: Private key material appears to be embedded in source. Private keys must never live in the repository. Remove it, store it in a secret manager, and rotate the key."
    },
    {
      "ruleName": "generic_hardcoded_secret",
      "origin": "psychiatry-ink",
      "severity": "medium",
      "regex": "(?i)(api[_-]?key|secret|client[_-]?secret|access[_-]?token|auth[_-]?token|passwd|password)\\s*[:=]\\s*[\\x22\\x27](?![^\\x22\\x27]*(process\\.env|import\\.meta\\.env|os\\.environ|getenv|\\$\\{))[^\\x22\\x27]{12,}[\\x22\\x27]",
      "reminder": "⚠️ Security Warning: A credential-looking value appears to be assigned a hardcoded string literal. If this is a real secret, load it from an environment variable / secret store instead of committing it. If it is a placeholder or non-secret, ignore this warning."
    },
    {
      "ruleName": "sql_string_concatenation",
      "origin": "psychiatry-ink",
      "severity": "high",
      "regex": "(?i)\\b(SELECT|INSERT\\s+INTO|UPDATE|DELETE\\s+FROM)\\b[^;\\n]{0,200}(\\+\\s*[a-zA-Z_$]|\\$\\{|\\bf[\\x22\\x27]|%\\s*\\(|\\.format\\()",
      "reminder": "⚠️ Security Warning: This looks like a SQL query built by string concatenation/interpolation, a classic SQL-injection sink. Use parameterized queries / bound parameters (e.g. prepared statements, an ORM with placeholders) instead of interpolating values into the SQL string."
    },
    {
      "ruleName": "weak_hash_for_security",
      "origin": "psychiatry-ink",
      "severity": "medium",
      "substrings": [
        "hashlib.md5(",
        "hashlib.sha1(",
        "createHash('md5')",
        "createHash(\"md5\")",
        "createHash('sha1')",
        "createHash(\"sha1\")"
      ],
      "reminder": "⚠️ Security Warning: MD5/SHA-1 are broken for security purposes (collisions, fast brute force). For password hashing use bcrypt/scrypt/Argon2; for integrity/HMAC use SHA-256+. MD5/SHA-1 are only acceptable for non-security checksums — note that in a comment if so."
    },
    {
      "ruleName": "insecure_random_for_secrets",
      "origin": "psychiatry-ink",
      "severity": "medium",
      "substrings": [
        "Math.random("
      ],
      "reminder": "⚠️ Security Warning: Math.random() is NOT cryptographically secure. If this value is a token, session id, password, nonce, OTP, or any secret, use crypto.getRandomValues() / crypto.randomBytes() / secrets (Python) instead. If it is only for non-security UI randomness, ignore this warning."
    }
  ]
}
